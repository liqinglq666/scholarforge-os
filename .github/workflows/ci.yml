name: Diagnose ScholarForge Scope Payload

on:
  pull_request:
    branches: [main]

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout redesign branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}

      - name: Verify payload chunks
        shell: bash
        run: |
          set -euo pipefail
          wc -c .github/scope-patch-*.b64
          sha256sum .github/scope-patch-*.b64
          cat .github/scope-patch-*.b64 > /tmp/scope-all.b64
          wc -c /tmp/scope-all.b64
          sha256sum /tmp/scope-all.b64
          base64 --decode /tmp/scope-all.b64 > /tmp/scope-reduction.tgz
          wc -c /tmp/scope-reduction.tgz
          sha256sum /tmp/scope-reduction.tgz
